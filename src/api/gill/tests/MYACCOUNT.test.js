import { POST } from "../apiClient";
jest.mock("../apiClient");
beforeEach(() => {
  jest.resetAllMocks();
  jest.mock("../apiClient");
});

describe("MYACCOUNT", () => {
  it("TODO", async () => {
    expect(true);
  });
});
