import { POST } from "../apiClient";
jest.mock("../apiClient");
beforeEach(() => {
  jest.resetAllMocks();
  jest.mock("../apiClient");
});

describe("USERRIGHTS", () => {
  it("TODO", async () => {
    expect(true);
  });
});
