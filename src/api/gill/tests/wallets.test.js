import { GET, POST } from "../apiClient";
jest.mock("../apiClient");
beforeEach(() => {
  jest.resetAllMocks();
  jest.mock("../apiClient");
});

describe("wallets", () => {
  it("TODO", async () => {
    expect(true);
  });
});
